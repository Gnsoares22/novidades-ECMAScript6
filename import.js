function calcAreaCircunferencia(raio){
    const PI = 3.14;
    return Math.pow(raio,2) * PI
}

const resultado = calcAreaCircunferencia(2);
console.log(resultado);

export default calcAreaCircunferencia; //padrao
